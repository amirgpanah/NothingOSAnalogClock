# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# VS Code (personal editor config, not shared)
.vscode/

# Node / tooling leftovers (no build step, but keep the repo clean)
node_modules/
dist/
.DSH/
*.log

# Dev-only: browser touch regression tooling (see check-touch.cjs), not needed to run the app
package.json
package-lock.json
